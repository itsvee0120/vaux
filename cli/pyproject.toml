[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "vaux-cli"
version = "0.2.9"
description = "A terminal client for vaux listening rooms."
readme = "README.md"
requires-python = ">=3.11"
authors = [
    { name = "Violet Nguyen", email = "nviolet0120@gmail.com" },
]
license = "MIT"
dependencies = [
    "click",
    "textual",
    "python-socketio[asyncio_client]",
    "httpx",
    "yt-dlp>=2026.8.19",
    "pyperclip",
    "pynacl>=1.5.0",
]

[project.urls]
Homepage = "https://github.com/itsvee0120/vaux"
Changelog = "https://github.com/itsvee0120/vaux/releases"
Issues = "https://github.com/itsvee0120/vaux/issues"

[project.scripts]
vaux = "main:cli"

[tool.setuptools]
packages = ["vaux"]
py-modules = ["main"]

[tool.setuptools.package-data]
vaux = ["room_slug_words.json"]